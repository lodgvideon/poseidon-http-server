apiVersion: v1
kind: ServiceAccount
metadata:
  name: poseidon-server
  labels:
    app.kubernetes.io/name: poseidon-server
    app.kubernetes.io/instance: poseidon-server
# The server does not call the k8s API; do not mount its token.
automountServiceAccountToken: false
